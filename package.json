{
  "name": "koco-url-utilities",
  "version": "1.3.0",
  "homepage": "https://github.com/cbcrc/koco-url-utilities",
  "authors": [
    "fcharest <frederik.charest@radio-canada.ca>",
    "Maxime Séguin <maxime.forget-seguin@radio-canada.ca>"
  ],
  "description": "URLs utilities to be used inside a koco project.",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "keywords": [
    "url",
    "utilities",
    "koco"
  ],
  "main": "src/url-utilities.js",
  "license": "MIT",
  "dependencies": {
    "koco-array-utilities": "^1.1.0",
    "koco-query": "^1.1.0",
    "koco-string-utilities": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
